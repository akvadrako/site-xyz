---
layout: default
---
<script>
    import {JsonView, Photo} from '$comp'
    import {loadWorks} from '$lib/metadata'
    import {lang} from '$lib'
    import {keyBy} from 'lodash-es'

    let kinds = [
        {
            key: 'illustration',
            label: { en: 'Illustrations', nl: 'Illustraties' },
            checked: true,
        },
        {
            key: 'mural',
            label: { en: 'Murals', nl: 'Muurschilderingen' },
            checked: true,
        },
        {
            key: 'painting',
            label: { en: 'Paintings', nl: 'Schilderijen' },
            checked: true,
        },
    ]

    let works = loadWorks($lang)

    $: bykey = keyBy(kinds, 'key')
    $: filtered = works.filter(w => bykey[w.kind || 'mural'].checked)
</script>

<style>
    section {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin: 10px 0;
    }
    a.work {
        text-decoration: none;
        display: inline-block;
        @apply py-8 px-8 w-sm h-sm mx-auto bg-white rounded-xl shadow-md space-y-2;
        @apply sm:(py-4 flex items-center space-y-0 space-x-6)
    }
    x-grid {
        display: grid;
        height: 100%;
        width: 100%;
    }
    x-grid div {
        grid-area: 1 / 1 / 1 / 1;
        margin: 0;
        padding: 0;
    }
    h3 {
        display: none;
        grid-area: 1 / 1 / 1 / 1;
        z-index: 10;
        background: #EEEEEE99;
        color: black;
        justify-self: center;
        align-self: center;
        margin: 0;
        padding: 10px;
    }
    a.work:hover h3 {
        display: block;
    }
    h2 {
        font-size: 200%;
        margin-top: 10px;
    }
</style>

[en]You've found yourself wanting a wall to wall service. So please
rech out by <b>approaching the [[Contact|About]] pagey.</b>

[nl]NL intro [[Contact]]

<h2>{filtered.length} of {works.length} works</h2>

<div class="flex items-left mb-4">
{#each kinds as kind}
    <div>
        <input
            id={kind.key}
            bind:checked={kind.checked}
            type="checkbox"
            class="w-4 h-4 text-blue-600 bg-gray-100 rounded border-gray-300 focus:ring-blue-500 dark:focus:ring-blue-600 dark:ring-offset-gray-800 focus:ring-2 dark:bg-gray-700 dark:border-gray-600"
        />
        <label for={kind.key} class="ml-2 mr-4">{kind.label[$lang]}</label>
    </div>
{/each}
</div>

<section>
    {#each filtered as work}
        <a
            class="work"
            href={work.path}>
            <x-grid>
                <h3 class="text-lg text-black font-semibold">{work.title}</h3>
                <div>
                    <Photo src={work.image} alt={work.title} sizes="400px" />
                </div>
            </x-grid>
        </a>
    {/each}
</section>

<p>
Prices: €1000 - €4000 / m²
</p>

